# Word Snap has moved

The Word Snap app now lives at [`/word-snap/`](../word-snap/) (online: <https://guojiz.github.io/word-snap/>).

This directory only keeps redirects for old links, plus a self-unregistering service worker that cleans up caches from the previous `/word-match/` install.

Word Snap 应用已迁移至 [`/word-snap/`](../word-snap/)（在线地址：<https://guojiz.github.io/word-snap/>）。此目录仅保留旧链接的重定向，以及一个用于清理旧缓存、自动注销的 service worker。
